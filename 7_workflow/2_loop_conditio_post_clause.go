// You can edit this code!
// Click here and start typing.
package main

func main() {
	//var i int
	for i := 0; i < 5; i++ { //We cannot reference i outside the scope of the foor loop if we do this. for initialization; condition; statment to execute AFTER every loop iteration.
		println(i)
	}
}
